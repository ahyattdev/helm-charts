apiVersion: v1
kind: Service
metadata:
  name: {{ include "acme-http01-forwarder.fullname" . }}
  labels:
    {{- include "acme-http01-forwarder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "acme-http01-forwarder.selectorLabels" . | nindent 4 }}
