apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acme-http01-forwarder.fullname" . }}-test-connection"
  labels:
    {{- include "acme-http01-forwarder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acme-http01-forwarder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
