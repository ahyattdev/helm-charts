apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syncserver-token.fullname" . }}-test-connection"
  labels:
    {{- include "syncserver-token.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "syncserver-token.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
