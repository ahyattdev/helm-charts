apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncserver-token.fullname" . }}
  labels:
    {{- include "syncserver-token.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syncserver-token.selectorLabels" . | nindent 4 }}
