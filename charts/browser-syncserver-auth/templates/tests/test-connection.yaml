apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syncserver-auth.fullname" . }}-test-connection"
  labels:
    {{- include "syncserver-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "syncserver-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
