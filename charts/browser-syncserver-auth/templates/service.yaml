apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncserver-auth.fullname" . }}
  labels:
    {{- include "syncserver-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syncserver-auth.selectorLabels" . | nindent 4 }}
